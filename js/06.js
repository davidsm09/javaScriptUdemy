//Objeto .math

let num;

num = Math.random(); //.Math.random nos permite generar numeros aleatorios

num = Math.PI; // nos genera el numero pi

num = Math.round(3.5); //nos permite redondear decimales

num = Math.ceil(2.1); // nos redondea hacia arriba siempre

num = Math.floor(2.1) // nos redondea hacia abajo siempre

num = Math.sqrt(144); //nos permite encontrar la raiz cuadrada

num = Math.abs(-200); //nos permite convertir numeros de negativo a positivo

num = Math.min(1, 5, 0, 6, 7, 4, 10); //nos permite encontrar el minimo de una lista de numeros

num = Math.max(1, 5, 0, 6, 7, 4, 10); // encontramos el valor maximo de una lista de numeros

num = Math.floor( Math.random() * 30 );



console.log(num);

